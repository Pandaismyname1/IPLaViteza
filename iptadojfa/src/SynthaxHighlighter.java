public class SynthaxHighlighter extends Plugin {

    public SynthaxHighlighter() {
        System.out.println("C - I'm just a Syntax Highlighting Plugin");
        FileManager.getInstance().saveFile();
        FileManager.getInstance().saveFile();
    }
}